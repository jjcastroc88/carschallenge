describe("Main", function() {
  var Main = require('../js/Main');

  beforeEach(function() {
    cat = new Cat();
  });
  it("the object should be undefined", function() {
    expect(candy)not
  });

});
